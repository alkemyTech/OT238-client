<resources>
    <!-- Base application theme. -->
    <style name="Theme.ONGAndroid" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- Text appearance label large -->
        <item name="textAppearanceLabelLarge">@style/TextAppearance.MaterialComponents.Body1</item>
    </style>

    <style name="Theme.ONGAndroid.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground"> @drawable/splash_background</item>
    </style>
    <style name="Theme.ONGAndroid.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.ONGAndroid.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Button.Icon" parent="Widget.Material3.Button.Icon">
        <item name="android:backgroundTint">@color/color_states_materialbutton</item>
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="rippleColor">@color/rippleButton</item>
        <item name="strokeColor">@color/strokeColor</item>
        <item name="strokeWidth">1dp</item>
        <item name="iconTint">@null</item>
    </style>

</resources>